import { RouteLoadingCard } from "@/components/shared/route-loading-card";

export default function Loading() {
  return (
    <RouteLoadingCard
      title="Cargando acceso"
      description="Preparando el formulario de ingreso..."
    />
  );
}
